<% content_for :title, "KBC Game #{game.id}" %>
<%= turbo_stream_from "game_#{game.id}" %>
<%= render partial: "games/game", locals: { game:, my_player: } %>
<%= javascript_include_tag "gameboard" %>
